// const nome = 'Henrry';
// const obj = { nome };
// const novoObj = { ...obj };

// console.log(novoObj);

class Person {
  constructor(name, surname) {
    this.name = name;
    this.surname = surname;
  }
}